# OAuth de Microsoft Graph (Hotmail). Trae requests, PyJWT y cryptography.
msal==1.35.1

# Lectura de extractos. Ya los usa reconciliacion/api/, se dejan aqui para que
# la Fase 5 no necesite reconstruir la imagen.
openpyxl==3.1.5
pdfplumber==0.11.10
